1.  component Container is just a container we are not 
    using any other container in the compoenent container as prop so we have to use the componentContainer Code repeatedly
2.  react-native-elements does not work in case of checkBox